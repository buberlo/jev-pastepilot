# Future server-side live provider only. MS2 and the Share-slice use the
# offline mock or local adapter and do not read these values. Never commit a
# real key. Never log TYPESAFE_API_KEY.
DECISION_PROVIDER=mock
TYPESAFE_API_KEY=
TYPESAFE_MODEL=
