# Future server-side live provider only. Milestone 2 uses the offline
# mock or local adapter and does not read these values. Never commit a real key.
# Never log TYPESAFE_API_KEY.
DECISION_PROVIDER=mock
TYPESAFE_API_KEY=
TYPESAFE_MODEL=
